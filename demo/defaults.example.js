// Template for local demo defaults.
//
// Copy this file to demo/defaults.local.js (which is gitignored) and
// replace the values with your own SAT and destination. The demo HTML
// loads defaults.local.js on startup — if it isn't present the demo
// still works but you'll have to paste credentials each time.
window.__SW_ADDRESS_DEMO_DEFAULTS__ = {
  token: '',
  destination: '/public/my-agent'
};
